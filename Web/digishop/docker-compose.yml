version: "3.8"

services:
  digishop:
    build: .
    container_name: digishop
    ports:
      - "4002:4002"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4002/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
