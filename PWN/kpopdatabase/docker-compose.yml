version: '3.8'
services:
  kpop:
    build: .
    container_name: kpop-db-pwn
    ports:
      - "4010:4010"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "bash", "-c", "</dev/tcp/localhost/4010"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
