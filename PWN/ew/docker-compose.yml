version: "3.8"

services:
  ew:
    build:
      context: ./src
      dockerfile: ./Dockerfile
    container_name: ew
    ports:
      - "4009:4009"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD", "bash", "-c", "</dev/tcp/localhost/4009"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
